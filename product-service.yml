server:
  port: ${PORT:8083}
spring:
  data:
    mongodb:
      authentication-database: admin
      uri: mongodb+srv://bruno:bruno@cluster0.lfrvs.mongodb.net/product?retryWrites=true&w=majority
managment:
  endpoint:
    web:
      exposure:
        include:
